name: Oracle Cloud Instance Bot

on:
  workflow_dispatch:
  schedule:
    - cron: '*/5 * * * *' # Her 5 dakikada bir çalışır

jobs:
  run-bot:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.1'

      - name: Install Dependencies
        run: composer install

      # 1. ADIM: Base64 kodunu çözüp dosyaya yazıyoruz
      - name: Create Key File
        run: |
          echo "${{ secrets.OCI_PRIVATE_KEY }}" | base64 --decode > oci_api_key.pem
          chmod 600 oci_api_key.pem

      # 2. ADIM (YENİ): Anahtar dosyasının doğru oluşup oluşmadığını kontrol ediyoruz
      # Merak etme, sadece ilk/son satırı ve satır sayısını gösterir, güvenlidir.
      - name: Debug Key File
        run: |
          ls -l oci_api_key.pem
          echo "First line:"
          head -n 1 oci_api_key.pem
          echo "Last line:"
          tail -n 1 oci_api_key.pem
          echo "Line count:"
          wc -l oci_api_key.pem

      - name: Run Bot
        env:
          OCI_USER_ID: ${{ secrets.OCI_USER_ID }}
          OCI_TENANCY_ID: ${{ secrets.OCI_TENANCY_ID }}
          OCI_KEY_FINGERPRINT: ${{ secrets.OCI_KEY_FINGERPRINT }}
          OCI_REGION: ${{ secrets.OCI_REGION }}
          OCI_PRIVATE_KEY_FILENAME: "oci_api_key.pem"
          OCI_SUBNET_ID: ${{ secrets.OCI_SUBNET_ID }}
          OCI_IMAGE_ID: ${{ secrets.OCI_IMAGE_ID }}
          OCI_OCPUS: ${{ secrets.OCI_OCPUS }}
          OCI_MEMORY_IN_GBS: ${{ secrets.OCI_MEMORY_IN_GBS }}
          OCI_AVAILABILITY_DOMAIN: ${{ secrets.OCI_AVAILABILITY_DOMAIN }}
          OCI_SSH_PUBLIC_KEY: ${{ secrets.OCI_SSH_PUBLIC_KEY }}
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: php index.php
